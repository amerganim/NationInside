import ComingNext from "@/components/app/ComingNext";
export const dynamic = "force-dynamic";
export default function TasksPage() {
  return <ComingNext title="My Tasks" desc="Missions assigned by your leaders — complete them, upload geo-tagged proof, and earn activity points." />;
}
