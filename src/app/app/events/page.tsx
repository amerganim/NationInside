import ComingNext from "@/components/app/ComingNext";
export const dynamic = "force-dynamic";
export default function EventsPage() {
  return <ComingNext title="Events" desc="See upcoming rallies and meetings, and check in with a QR scan to record your attendance." />;
}
